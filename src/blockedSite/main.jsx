import React from 'react';
import ReactDOM from 'react-dom/client';
import BlockedSiteApp from './BlockedSiteApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <BlockedSiteApp />
    </React.StrictMode>
); 